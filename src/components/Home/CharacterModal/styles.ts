import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  image: {
    width: 200,
    height: 200,
    resizeMode: 'cover',
    alignSelf: 'center',
  },
  text: {
    textAlign: 'center',
    fontSize: 20,
  },
});
