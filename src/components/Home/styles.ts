import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  container: {
    marginBottom: 90,
  },
  logo: {
    width: '100%',
    height: 50,
    resizeMode: 'contain',
    marginBottom: 10,
  },
  flatList: {marginBottom: 90},
});
