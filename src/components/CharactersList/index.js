import React from 'react';

import { Container } from './styles';

const CharactersList = ({ character }) => {
  const goToCharacter = () => {};

  return <Container onPress={goToCharacter} />;
};

export default CharactersList;
