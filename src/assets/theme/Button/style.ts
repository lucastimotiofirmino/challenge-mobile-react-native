import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  button: {
    paddingVertical: 20,
    borderRadius: 6,
  },
  text: {
    textAlign: 'center',
    fontWeight: '600',
    fontSize: 18,
  },
});
