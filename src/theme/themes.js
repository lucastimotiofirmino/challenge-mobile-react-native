import Colors from './colors';

import Sizes from './fonts';

const light = {
  primary: Colors.RED,
  secondary: Colors.BLACK,
  background: Colors.BACKGROUND_EEE,
  backgroundDark: Colors.DDD,
  text: Colors.BLACK,
  sizes: Sizes,
};

const dark = {
  primary: Colors.RED,
  secondary: Colors.BLACK,
  background: Colors.BLACK,
  backgroundDark: Colors.BLACK,
  text: Colors.WHITE,
  sizes: Sizes,
};

export default { light, dark };
