import Colors from './colors';

const light = {
  primary: Colors.RED,
  secondary: Colors.BLACK,
  background: Colors.BACKGROUND_EEE,
  text: Colors.BLACK,
};

const dark = {
  primary: Colors.RED,
  secondary: Colors.BLACK,
  background: Colors.BLACK,
  text: Colors.WHITE,
};

export default { light, dark };
