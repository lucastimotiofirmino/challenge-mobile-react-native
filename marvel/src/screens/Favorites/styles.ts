import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: 20,
  },
  hw100: {
    height: '100%',
    width: '100%',
  },
});

export default styles;
